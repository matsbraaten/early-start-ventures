# Local, machine-specific files that should not be published
.claude/settings.local.json

# Original full-size photos (multi-MB) — only the optimized web copies
# (headshot.jpg, toronto-skyline.jpg) are published
images/torstein-headshot.jpg
images/*-unsplash.jpg
